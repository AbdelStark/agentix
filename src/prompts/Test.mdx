TESTING PHASE — Unit: {props.unitId}

Title: {props.unitName}
Category: {props.unitCategory}

## Executable Specification (BDD)

Feature: {props.gherkinFeature}
{props.gherkinScenarios && props.gherkinScenarios.length > 0
  ? props.gherkinScenarios.map((s, idx) => `### Scenario ${idx + 1}: ${s.title} (${s.id})\nGiven:\n${(s.given || []).map((g) => `- ${g}`).join('\n')}\nWhen:\n${(s.when || []).map((w) => `- ${w}`).join('\n')}\nThen:\n${(s.then || []).map((t) => `- ${t}`).join('\n')}`).join('\n\n')
  : 'No scenarios provided'}

Domain invariants:
{props.domainInvariants && props.domainInvariants.length > 0 ? props.domainInvariants.map((inv, i) => `${i + 1}. ${inv}`).join('\n') : '- None provided'}

{props.whatWasDone ? `\n## What was implemented\n${props.whatWasDone}` : ''}
{props.filesCreated && props.filesCreated.length > 0 ? `\nFiles created: ${props.filesCreated.map(f => `- ${f}`).join('\n')}` : ''}
{props.filesModified && props.filesModified.length > 0 ? `\nFiles modified: ${props.filesModified.map(f => `- ${f}`).join('\n')}` : ''}

Run ALL of the following test categories and report results:

{props.testSuites && props.testSuites.length > 0
  ? props.testSuites.map((suite, i) => `## ${i + 1}. ${suite.name}\nRun: \`${suite.command}\`\n${suite.description}\n${suite.skipCondition ? `If ${suite.skipCondition}, note "${suite.skipNote || 'N/A'}".` : ''}`).join('\n\n')
  : '## Run all test suites\nExecute all relevant test commands for this project.'}

## TDD Verification
- Check that test files exist for new code
- Check that integration tests exist for new user-facing features
- If tests were written AFTER implementation (not TDD), flag as an issue
- Produce scenario coverage:
  - `scenariosTotal`: total number of Gherkin scenarios
  - `scenariosCovered`: how many have direct automated test coverage
  - `uncoveredScenarios`: list scenario IDs still uncovered
  - `scenarioTrace`: one entry per scenario: `{ scenarioId, mappedTests, evidence }`
    - `mappedTests`: concrete test identifiers (`path::testName` preferred)
    - `evidence`: `{ given, when, then }` notes proving observed behavior
  - `traceCompleteness`: `true` only if every scenario has at least one mapped test
  - `assertionSignals`: `{ totalAssertions, filesWithAssertions, weakTestsDetected }`
  - `antiSlopFlags`: machine-readable flags (empty when clean)
  - `tddEvidence`: concise explanation of how RED->GREEN->REFACTOR was followed
  - `scenarioCoverageNotes`: short narrative on coverage quality and gaps
- Anti-slop checks (must be reflected in `antiSlopFlags` when found):
  - scenario without mapped tests
  - behavior-changing implementation with no test file changes
  - weak tests (placeholder/todo/skip-only/empty-assertion patterns)
- If any scenario is uncovered, trace is incomplete, or blocking anti-slop flags exist, treat the unit as NOT ready for merge.

If any tests fail and you need to fix code to make them pass, commit each fix atomically:
- `jj describe -m "{props.fixCommitPrefix || '🐛 fix'}(SCOPE): what was fixed"`
- `jj new`
- After fixing: {`jj bookmark set ${props.branchPrefix || "unit/"}${props.unitId} -r @`} && {`jj git push --bookmark ${props.branchPrefix || "unit/"}${props.unitId}`}

Report each category: passed or failed with details.
